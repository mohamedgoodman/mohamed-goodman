# Resend API key for the contact form (https://resend.com/api-keys).
# Leave unset to use the mock contact API route (logs submissions, returns
# success) — useful for local dev and preview deployments.
RESEND_API_KEY=
